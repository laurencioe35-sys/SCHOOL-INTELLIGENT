# Voice confidentiality

El canal de voz comparte las mismas restricciones de acceso que la pantalla y
ademas aplica reglas por campo. Un permiso valido para ver un saldo no revela
un numero de cuenta bloqueado. Si la consulta real no devuelve datos, el
asistente responde que no puede consultar; nunca inventa cifras.